# Prediction of house pricing using Linear regression 

	Used sklearn to import the dataset
	Used pandas and numpy to normalise the dataset
	Used gradient descent algorithm to minimize the error function
	

